ZO_SavedVars = {
    NewAccountWide = function(
            savedVariableTable,
            version,
            namespace,
            defaults,
            profile,
            displayName
    )
        dd('error. should be not have been called.')
    end
}


